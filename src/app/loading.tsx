"use client";
import LoadingScreen from "@/components/LoadingScreen";

export default function Loading() {
  // Next.js will handle mounting/unmounting this component during route transitions
  return <LoadingScreen />;
}
